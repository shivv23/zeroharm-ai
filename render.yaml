services:
  - type: web
    name: zero-harmer-backend
    env: python
    region: ohio
    plan: starter
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: cd backend && uvicorn api.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: WS_UPDATE_INTERVAL
        value: 2
      - key: RISK_TREND_MAX
        value: 60
      - key: JWT_SECRET
        generateValue: true
      - key: API_KEY
        generateValue: true
      - key: DATABASE_URL
        value: sqlite+aiosqlite:///data/zeroharm.db
      - key: JWT_EXPIRY_HOURS
        value: 24

static_sites:
  - name: zero-harmer-frontend
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: frontend/build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: REACT_APP_WS_URL
        value: wss://zero-harmer-backend.onrender.com/ws
      - key: REACT_APP_API_URL
        value: https://zero-harmer-backend.onrender.com/api
